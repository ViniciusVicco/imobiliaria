# design_system

Pacote de design/layout compartilhado.

Inclui:
- Breakpoints padrao de responsividade
- Builder global com `responsive_framework`
- Container de pagina com largura maxima para desktop
- Export de dependencias visuais (`flutter_svg`, `cached_network_image`)
